# Secure Vault

Secure Vault is a small client-side password vault built with Next.js.

All data is encrypted directly in the browser before being saved. There is no backend, no API, and no network storage involved.

---

## Running the project

Install dependencies and start the dev server:

```bash
npm install
npm run dev
```
